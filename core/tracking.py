from __future__ import annotations

from typing import List, Dict, Any


def assign_tracks(frames: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Placeholder tracking that assumes person_id already assigned."""
    return frames
